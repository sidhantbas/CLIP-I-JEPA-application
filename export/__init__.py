"""The vitrine's loading dock: one entry script, three helper crates.

for_the_vitrine.py is the single permitted export entry point; it leans
on onnx_bodies (tracing adapters), readouts (regrown ephemerals) and
exhibits (serialization) so every file honors the 250-line rule.
"""
